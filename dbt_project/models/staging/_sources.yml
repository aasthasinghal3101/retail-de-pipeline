version: 2

sources:
  - name: raw
    description: "Raw landing zone tables loaded as-is from data/raw/*.csv by ingestion/load_to_warehouse.py"
    schema: raw
    tables:
      - name: customers
      - name: products
      - name: orders
      - name: order_items
